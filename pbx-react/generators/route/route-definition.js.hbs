{{!--

This template is for adding a react router <Route> component with our newly generate path and route component.

--}}
                    <Route
                        path={{{ openingCurly }}}routePaths.{{camelCase componentName}}{{{ closingCurly }}}
                        element={{{ openingCurly }}}<{{pascalCase componentName}}Route />{{{ closingCurly }}}
                    />
